// pinverified

export default function() {
  // var element = document.getElementById('keypadOverlay')
  // var style = window.getComputedStyle(element)
  // Show the key pad
  // if (style.removeProperty) {
  //   style.removeProperty('display')
  // } else {
  //   style.removeAttribute('display')
  // }

  // // var display = style.getPropertyValue('display')
  // // document.getElementById('keypadOverlay').style.display = '' //show the key pad
  // // document.getElementById('splashId').style.display = '' //show the splash screen with key pad

  //TEMP to disable this test
  if (true) {
    // Hide splash screen now
    return true //pin entered okay
  }
  // Flash the key pad red (give more trys?)
  return false //can not continue
}
