import React, { Component } from 'react';
import BetaNotice from '../BetaNotice';

class Close extends Component {
    render() {
        return (
            <BetaNotice pageName={"Close App"} />
        );
    }
}

export default Close