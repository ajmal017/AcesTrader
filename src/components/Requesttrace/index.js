import React, { Component } from 'react';
import BetaNotice from '../BetaNotice';

class Requesttrace extends Component {
    render() {
        return (
            <BetaNotice pageName={"Request Trace"} />
        );
    }
}

export default Requesttrace