import React, { Component } from 'react';
import BetaNotice from '../BetaNotice';

class Readtracelog extends Component {
    render() {
        return (
            <BetaNotice pageName={"Read Trace Log"} />
        );
    }
}

export default Readtracelog