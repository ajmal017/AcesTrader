import React, { Component } from 'react';
import BetaNotice from '../BetaNotice';

class Readerrorlog extends Component {
    render() {
        return (
            <BetaNotice pageName={"Read Error Log"} />
        );
    }
}

export default Readerrorlog