import React, { Component } from 'react';
import BetaNotice from '../BetaNotice';

class Exit extends Component {
    render() {
        return (
            <BetaNotice pageName={"Exit"} />
        );
    }
}

export default Exit